// The implementation of this declaration file is
// auto-generated by scripts/filters/ui_composite.ts

/**
 * The short IDs (`ui_${icon}_${tileX}_${tileY}`) of the special icon tiles that are
 * fully transparent. Those tiles render as nothing, so the `ui_composite` build filter
 * generates no item or texture for them.
 */
declare const TRANSPARENT_ICON_TILES: ReadonlySet<string>;
/**
 * The short ID of each special icon tile that shares its texture with an earlier tile,
 * mapped to the short ID of that tile. The `ui_composite` build filter generates no
 * item or texture of its own for these.
 */
declare const ICON_TILE_REDIRECTS: Readonly<Record<string, string>>;
export { TRANSPARENT_ICON_TILES, ICON_TILE_REDIRECTS };
